// This is a constant and it cannot change in value
const x = 2;
// this is a variable and it can change in value
let y = 5;

// we are making operations here with the numbers we
// just declared
const addition = x + y;
const substraction = x - y;
const division = x / y;
const multiplication = x * y;
const randomValue = ((x + y)*10) / x;

console.log(addition);
console.log(substraction);
console.log(division);
console.log(multiplication);

console.log(randomValue);